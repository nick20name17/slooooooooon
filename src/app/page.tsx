import { Button } from '@/components/ui/button'

const Home = () => {
    return (
        <main className='flex min-h-screen items-center justify-center'>
            <Button>Чайна лавка блаблаблабал</Button>
        </main>
    )
}

export default Home
